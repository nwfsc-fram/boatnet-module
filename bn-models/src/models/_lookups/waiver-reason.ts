import { BaseLookup } from '../_base';

export const WaiverReasonTypeName = 'waiver-reason';

export type WaiverReason = BaseLookup;
// notes:
// description?: string; // Observer Availability, Vessel Safety, ...
// isActive?: boolean
// lookupVal?: number; // 1-11